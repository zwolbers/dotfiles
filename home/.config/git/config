[user]
    # email purposely unset.  Add to project .git/config for each repo.
    name = Zach Wolbers
[color "grep"]
    filename = magenta
    linenumber = green
    function = yellow
[core]
    pager = cat
[diff]
    tool = meld
[alias]
    lg = log --graph --all --date=format-local:'%F %R' --pretty=tformat:'%C(auto)%h %C(magenta)%ad%C(auto)%d %s' -n 25
[url "https://aur.archlinux.org/"]
    insteadOf = "aur:"
